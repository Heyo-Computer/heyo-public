+++
id = "T-040"
title = 'Add a mode "verbose" that reports agent tool usage and review feedback'
status = "done"
priority = 3
created_at = "2026-05-29T18:01:18Z"
updated_at = "2026-05-29T18:10:42Z"
owner = "sarocu"
labels = []
depends_on = ["T-043"]
blocked_reason = ""
spec_anchor = "580daa4e25af1335"
+++
## Notes

- 2026-05-29T18:04:19Z [sarocu] PLAN — verbose mode reporting agent tool usage + review feedback. Depends on T-043 (stream-json parser).

Current state: `-v/--verbose` exists on Run/Plan/Review/Exec (cli.rs) but only drives the heartbeat/spinner (session.rs heartbeat_loop) — it does NOT surface tool calls (claude runs single-object json, no per-tool visibility). Review feedback IS already printed (review.rs:236 prints report; verdict at :259) and written to followups; exec do_review prints verdict per pass.

Part A — tool usage (needs T-043):
- Once T-043 lands parse_claude_stream + TurnOutcome.tools, gate it on `verbose`: AgentInvocation already carries `verbose`; have claude_argv use the streaming form when verbose.
- session.rs Session::turn: after parse_outcome, when self.verbose && !outcome.tools.is_empty(), print a compact `[printer] tools: Read x3, Bash x2, Edit x1` summary line per turn (aggregate by name). Optionally per-call `[agent] ⚙ <tool>(<input_summary>)` lines.
- Thread through run.rs/review.rs (Session already built with .with_verbose(args.verbose)).

Part B — review feedback in verbose:
- exec.rs do_review: when args.verbose, after each pass echo a highlighted block — verdict + the parsed `## Suggested follow-ups` (reuse review::extract_section(&outcome.report, "## Suggested follow-ups")) — so the fed-back findings are visible in the live log, not only in the file.
- review.rs: when verbose, also print path of written followups (already does) + a one-line `## Verification` extract via extract_section.

Files: agent/mod.rs (argv gate), session.rs (per-turn tool summary), exec.rs do_review (feedback echo), review.rs (verbose extras). No new CLI flag — extends existing `-v`.

Verify:
- cargo test: session-level tool-summary aggregation helper (pure fn over Vec<ToolUseEvent>); extract_section already tested.
- manual: `printer run -v spec.md` shows per-turn tool summary; `printer exec -v spec.md` echoes verdict+follow-ups each review pass.

Coarse part (stream parsing) is split into T-043; this task is the reporting layer on top.
- 2026-05-29T18:10:42Z [sarocu] DONE — verbose reporting on top of T-043. Part A (tool usage): session.rs Session::turn, after parse_outcome, when self.verbose && !outcome.tools.is_empty() prints per-call '[agent] ⚙ <tool>(<input_summary>)' lines + an aggregated '[printer] tools: Read x3, Bash x2, Edit x1' line (new pure fn format_tool_summary, ordered by desc count then name; 2 unit tests). Imported ToolUseEvent. verbose already threaded to AgentInvocation+Session in run.rs/review.rs, so claude now runs stream-json under -v and tools populate. Part B (review feedback): exec.rs do_review echoes per-pass 'verdict … — suggested follow-ups:' via review::extract_section when args.verbose; review.rs review_with_sandbox echoes a '## Verification' stderr block under -v (full report still printed to stdout). No new CLI flag — extends existing -v. cargo build warning-free; full suite 145 passed. Not run as live 'printer run -v' e2e (needs authenticated claude + sandbox); covered by unit tests + reused extract_section tests.
