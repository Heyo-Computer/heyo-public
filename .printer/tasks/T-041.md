+++
id = "T-041"
title = "Add metrics reporting to a file to track token usage"
status = "done"
priority = 3
created_at = "2026-05-29T18:01:18Z"
updated_at = "2026-05-29T18:17:49Z"
owner = "sarocu"
labels = []
depends_on = []
blocked_reason = ""
spec_anchor = "70a8e1eee6e781ba"
+++
## Notes

- 2026-05-29T18:03:51Z [sarocu] PLAN — write token-usage metrics to a file.

TokenUsage already exists (agent/mod.rs: input_tokens/output_tokens/cache_creation/cache_read, input_total(), grand_total(), Display). It is aggregated per-Session (session.usage_total) and per-exec (exec.rs run_action `total`), but today only eprintln-d (run.rs:143, review.rs:260, exec.rs:821). Persist it.

Files & changes:
- agent/mod.rs: add `#[derive(Serialize)]` (serde) to TokenUsage so fields serialize. (Deserialize optional.)
- new module printer/src/metrics.rs (add `mod metrics;` to main.rs). Define:
    #[derive(Serialize)] struct MetricsRecord { ts: String (rfc3339), spec: String, phase: &str, agent: String, model: Option<String>, #[serde(flatten)] usage: TokenUsage, grand_total: u64 }
    pub fn append(cwd: &Path, rec: &MetricsRecord) -> Result<()>: create_dir_all(cwd/.printer); append one JSON line (NDJSON) to cwd/.printer/metrics.jsonl via OpenOptions append. Best-effort: callers log on Err, never fail the run.
- Wire call sites (compute phase + spec + agent string from args.agent.to_string() + model):
    * run.rs run_with_sandbox: after `if let Ok(usage) = &inner` -> metrics::append(&cwd, phase="run").
    * review.rs review_with_sandbox: after `let usage = session.usage_total` -> append(phase="review") using followups_cwd.
    * exec.rs run_action: after computing `total` -> append(phase="exec-total").
- Keep path fixed at .printer/metrics.jsonl for v1 (note: an optional `--metrics-file` override on RunArgs/ReviewArgs/ExecArgs could come later; not required to land).

Verify:
- cargo test: metrics.rs unit test — append twice to a tempdir, read back two NDJSON lines, serde_json parse each, assert grand_total + phase.
- cargo build; manual `printer exec spec.md` then inspect .printer/metrics.jsonl has run/review/exec-total rows.

Related to T-040 (both surface usage) but independent — no dependency. Lands in one pass.
- 2026-05-29T18:17:49Z [sarocu] DONE — persist token usage to .printer/metrics.jsonl (NDJSON).
- agent/mod.rs: derive Serialize on TokenUsage (import serde::Serialize).
- new src/metrics.rs (mod added to main.rs): MetricsRecord{ts(rfc3339),spec,phase,agent,model?,#[serde(flatten)]usage,grand_total}; append(cwd,&rec) creates .printer/ and append-writes one JSON line via OpenOptions; record(...) best-effort wrapper that logs on Err so a metrics write never fails a run; metrics_path() helper.
- Wired call sites: run.rs run_with_sandbox (phase="run", on Ok(usage)); review.rs review_with_sandbox (phase="review", uses followups_cwd); exec.rs run_action (phase="exec-total", spec extracted by-ref before the action match; AlreadyDone early-returns so no row for a no-op).
Verify: cargo build clean; cargo clippy clean for new code; cargo test -> 150 passed incl metrics::appends_ndjson_rows (asserts 2 NDJSON rows, flattened input_tokens, grand_total=sum, None model omitted, model=opus present). Full run/review/exec path wired but exercising it end-to-end needs a live agent.
