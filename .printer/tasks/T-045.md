+++
id = "T-045"
title = "Ensure the computer CLI is available wherever UI review runs"
status = "done"
priority = 3
created_at = "2026-05-29T18:19:03Z"
updated_at = "2026-05-29T18:26:56Z"
owner = "sarocu"
labels = []
depends_on = ["T-044"]
blocked_reason = ""
spec_anchor = ""
+++
Investigation T-042 finding: install.sh installs the 'computer' binary on the HOST (PRINTER_BINS default 'printer computer codegraph'), but the heyvm sandbox base image (ubuntu:24.04) never runs install.sh, so 'computer' is not on PATH inside the sandbox. Once T-044 decides where UI review runs: (host path) confirm 'computer' is on PATH and fail with a clear message if not; (sandbox path, if ever pursued) build+copy the computer bin into the image or install it via the driver's post_create. Depends on T-044 because the install target follows from where review runs.

## Notes

- 2026-05-29T18:21:25Z [sarocu] PLAN (session 2):
Depends on T-044, which routes UI review to the HOST. So the install target is the host, and install.sh already installs 'computer' to the host (PRINTER_BINS default 'printer computer codegraph', install.sh:35). No sandbox image change needed.

Work:
- In printer/src/review.rs, on the T-044 host-routing branch (UI surface + host display, sandbox skipped), probe for the 'computer' binary on PATH before dispatching: small helper fn computer_on_path() -> bool using 'which'-style lookup (iterate PATH entries for an executable 'computer', or Command::new("computer").arg("--version")). 
- If absent: emit a clear, actionable warning to stderr — '[printer] computer CLI not found on PATH; UI click-testing will be skipped. Install it: re-run install.sh (or PRINTER_BINS=computer ./install.sh).' Do NOT hard-fail the review (still run static + build/test review); the missing-display/missing-tool case is gated by T-046.
- Sandbox path (not pursued by T-044) — leave a one-line note in code that building/copying 'computer' into the heyvm image would be the alternative; out of scope.

Verify: cargo test -p printer (helper finds a known bin, returns false for a bogus name); manual: PATH stripped of computer -> confirm the actionable warning fires and review still completes.
- 2026-05-29T18:26:56Z [sarocu] DONE (session 3): T-044 routes UI review to the HOST, so install target is the host; install.sh already installs 'computer' there (PRINTER_BINS default). No sandbox image change.
- review.rs: added computer_on_path() helper (scans PATH entries for an executable file 'computer'). In the T-044 host-routing branch (UI surface + host display, sandbox skipped), probe it; if absent emit actionable stderr warning ('computer CLI not found on PATH; UI click-testing will be skipped. Install it: re-run install.sh (or PRINTER_BINS=computer ./install.sh).') and continue — does NOT hard-fail (missing-display/tool case is gated by T-046).
- Code comment notes the sandbox-path alternative (build/copy computer into heyvm image) is out of scope.
- Test: computer_on_path_finds_known_bin (empty PATH->false, temp dir w/ exec 'computer'->true). cargo test -p printer: 154 passed.
