# Resolver for the email-alerts guest. init.sh copies this to /etc/resolv.conf
# at boot. Point it at whatever resolves your SMTP relay — build-image.sh
# rewrites this file when DNS_SERVER is set.
nameserver 8.8.8.8
