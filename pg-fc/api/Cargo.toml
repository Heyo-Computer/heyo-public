[package]
name = "pg-fc-api"
version = "0.1.0"
edition = "2024"
description = "Wire types for pg-vm-pool's JSON admin API, shared with its clients (app-lb's pg-fc plugin)."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
